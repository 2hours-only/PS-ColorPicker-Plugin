## 要求文件规则(本指令最为优先)
由于整体代码量较大，不可能一次提供所有的文件代码，因此有一些文件会被省略。如果你认为回答当前会话**有可能**需要用到被省略的文件，请直接终止本轮对话原本的任务并直接列出所有你需要看到的完整文件即可。注意，即使有可能同时存在不需要看到这些文件也能解决的方案，只要**有可能**要用到，就直接停止。

## 给出代码规则
0. **无论如何修改代码，不能删除任何已有的功能，哪怕你不知道这在做什么**
1. 给出代码时，如果文件较短（500行以内左右），请直接给出完整的文件代码。
例如下面这样的代码是可以的。
```
#ifndef MAP_H
#define MAP_H

#include <QWidget>
#include <QVector>
#include "place.h"
#include <QFile>
#include <QFileDialog>
#include <QDebug>
#include <QMessageBox>
#include <QJsonDocument>
#include <QJsonObject>
#include <QJsonArray>

using std::vector;

// ==================== 配置结构体定义 ====================

struct ZombieConfig {
    int blood = 100;
    double moveSpeed = 1.0;
    int attackPower = 10;
    int skillDuration = 0;
    int skillInterval = 0;
    double buffSpeed = 1.0;
    double buffAttack = 1.0;
};

struct TerrainConfig {
    double iceSpeedBuff = 1.5;
    double blackSoilDiscount = 0.3;
};

struct PlantConfig {
    int cost = 100;
    int blood = 100;
    int attackPower = 10;
    int attackRange = 100;
    int attackSpeed = 1;
    int blockUnits = 1;
    QString bullet = "";
    QString description = "";
};

// ==================== 游戏配置管理类 ====================

class GameConfig {
public:
    TerrainConfig terrainConfig;

    ZombieConfig normalZombieConfig;
    ZombieConfig flagZombieConfig;
    ZombieConfig ballonZombieConfig;
    ZombieConfig bucketZombieConfig;
    ZombieConfig splitZombieConfig;
    ZombieConfig miniZombieConfig;
    ZombieConfig bossZombieConfig;

    PlantConfig nutConfig;
    PlantConfig starfruitConfig;
    PlantConfig bottleConfig;
    PlantConfig spikeweedConfig;
    PlantConfig peashooterConfig;

    int sunshineDropRate = 150;
    int zombieSpawnInterval = 10000;

    bool loadGameConfig(const QString& filePath);
};

// ==================== 地图类 ====================

class Map : public QWidget {
    Q_OBJECT
public:
    explicit Map(QWidget *parent = nullptr);
    Map(QString p);
    bool loadMap(const QString& filePath);

    vector<vector<Place*>> scene;
    vector<vector<Place*>> landPaths;
    vector<vector<Place*>> flyPaths;

    int cellW = 100;
    int cellH = 100;
    int offsetX = 350;
    int offsetY = 180;
    int rows = 0;
    int cols = 0;
};

#endif // MAP_H

```

2. 如果文件较长，请以函数为最小单位给出代码，代码中不能省略任何部分。

例如下面的代码不可以，因为多处出现了省略。
```
void GameScene::mousePressEvent(QMouseEvent *event) {
    // ... 点击阳光和选择单位代码 ...

    if (event->position().x()>=map->offsetX && ...) {
        int x=(event->position().x()-map->offsetX)/map->cellW;
        int y=(event->position().y()-map->offsetY)/map->cellH;

        if (preparedPlant=="nut"){
            // 【修改】从配置读取 cost
            int cost = this->gameConfig->nutConfig.cost;
            if (map->scene[y][x]->terrainType == 3) cost = cost * (1.0 - this->gameConfig->terrainConfig.blackSoilDiscount);

            // ... 错误提示逻辑 ...

            else if (map->scene[y][x]->landPath && !map->scene[y][x]->firstPlace && isPlaceEmpty(y,x)){
                if (sunNum >= cost) {
                    qDebug()<<"成功种植nut";
                    // 【修改】从配置读取 blood
                    Nut * n = new Nut(y, x, this->gameConfig->nutConfig.blood, ":/res/nut.gif", this->gameConfig->nutConfig.cost);
                    createPlant(n);
                    preparedPlant="";
                }
                // ... 否则提示缺阳光 ...
            }
        }

        if (preparedPlant=="starfruit"){
            // 【修改】
            int cost = this->gameConfig->starfruitConfig.cost;
            if (map->scene[y][x]->terrainType == 3) cost = cost * (1.0 - this->gameConfig->terrainConfig.blackSoilDiscount);

            if (map->scene[y][x]->towerPos && isPlaceEmpty(y,x)){
                if (sunNum >= cost) {
                    qDebug()<<"成功种植starfruit";
                    StarFruit * n = new StarFruit(y, x, this->gameConfig->starfruitConfig.blood, ":/res/starfruitBullet.png", this->gameConfig->starfruitConfig.cost);
                    createPlant(n);
                    preparedPlant="";
                }
                // ...
            }
        }

        if (preparedPlant == "bottle") {
            // 【修改】
            int cost = this->gameConfig->bottleConfig.cost;
            if (map->scene[y][x]->terrainType == 3) cost = cost * (1.0 - this->gameConfig->terrainConfig.blackSoilDiscount);

            if (isPlaceEmpty(y,x)) {
                if (sunNum >= cost) {
                    qDebug() << "成功种植bottle at row:" << y << "col:" << x;
                    // 【修改】使用配置数据创建 Bottle
                    Bottle * b = new Bottle(y, x, 
                        this->gameConfig->bottleConfig.blood, 
                        this->gameConfig->bottleConfig.bullet, 
                        this->gameConfig->bottleConfig.cost, 
                        this->gameConfig->bottleConfig.attackPower, 
                        this->gameConfig->bottleConfig.attackRange, 
                        this->gameConfig->bottleConfig.attackSpeed, 
                        this->map);
                    createPlant(b);
                    preparedPlant = "";
                }
                // ...
            }
        }

        if (preparedPlant=="Spikeweed"){
            // 【修改】
            int cost = this->gameConfig->spikeweedConfig.cost;
            if (map->scene[y][x]->terrainType == 3) cost = cost * (1.0 - this->gameConfig->terrainConfig.blackSoilDiscount);

            if (map->scene[y][x]->landPath && !map->scene[y][x]->firstPlace && isPlaceEmpty(y,x)){
                if (sunNum >= cost) {
                    qDebug()<<"成功种植地刺";
                    // 【修改】传递 cost
                    Spikeweed * s = new Spikeweed(y, x, this->gameConfig->spikeweedConfig.cost);
                    createPlant(s);
                    preparedPlant="";
                }
                // ...
            }
        }

        // ... 铲子逻辑 ...
    }

    preparedPlant="";
}
```

3. 如果函数也非常长（大于500行左右），不适合给出整个函数，请明确指出从原函数的哪一句开始，到原函数的哪一句，要替换成什么。给出的替换代码同样不能有省略部分。必须给出起始位置和结束位置两个位置，不能只说从哪里开始而不说到哪里结束。

尤其注意，指明从原函数的哪一句开始，到原函数的哪一句结束的标识，必须用原代码，不能用类似```// 在 paintEvent 中，绘制 SpikeweedCard 之后添加```这种模糊表述。

例如下面的代码不可以，理由如下
- '// gamescene.cpp 中 bossZombie 创建部分'这种位置说明非常不明确，应当明确指出从原函数的哪一句开始(给出该位置代码便于搜索)。
- 中间出现了‘// ... 计算速度代码 ...’这种让人困惑的注释，难以合并代码。

```
// gamescene.cpp 中 bossZombie 创建部分
else if (type == "bossZombie") {
    qDebug() << "准备创建Boss僵尸，配置值: 间隔=" << this->gameConfig->bossZombieConfig.skillInterval 
              << " 持续=" << this->gameConfig->bossZombieConfig.skillDuration;

    double finalSpeed = 0.5;
    // ... 计算速度代码 ...

    bossZombie * b = new bossZombie(map->landPaths[i][0]->y*map->cellW+map->offsetX, 
                                    map->landPaths[i][0]->x*map->cellH+map->offsetY, 
                                    i, 
                                    this->gameConfig->bossZombieConfig.blood, 
                                    this->gameConfig->bossZombieConfig.attackPower);

    qDebug() << "Boss僵尸构造完成，内存地址=" << b;
    b->setMoveSpeed(finalSpeed);

    // 关键调试：设置技能参数
    qDebug() << "设置Boss技能参数: 原间隔=" << b->skillInterval 
              << " -> " << this->gameConfig->bossZombieConfig.skillInterval;
    b->skillDuration = this->gameConfig->bossZombieConfig.skillDuration;
    b->skillInterval = this->gameConfig->bossZombieConfig.skillInterval;
    b->buffSpeed = this->gameConfig->bossZombieConfig.buffSpeed;
    b->buffAttack = this->gameConfig->bossZombieConfig.buffAttack;

    qDebug() << "设置后Boss技能参数: 间隔=" << b->skillInterval << " 持续=" << b->skillDuration;

    z = b;
}
```
下面的代码也不可以，理由如下：
- 没有明确给出起始替换位置的原代码，只说了 ... spikeweedButton 代码 ...这个模糊表述
- 没有给出结束替换位置。

```
    // ... spikeweedButton 代码 ...

    QPushButton * icebottleButton = new QPushButton(this);
    icebottleButton->resize(90, 120);
    icebottleButton->move(470, 40);
    icebottleButton->setFlat(true);
    icebottleButton->setStyleSheet("QPushButton{border:none;background:transparent;}");
    connect(icebottleButton, &QPushButton::clicked, this, [=](){
        if (sunNum >= this->gameConfig->icebottleConfig.cost && timer->isActive()){
            preparedPlant = "icebottle";
        }
        if (sunNum < this->gameConfig->icebottleConfig.cost){
            qDebug() << "没有足够阳光种冰瓶子炮";
            Message * m = new Message(620,40,":/res/lackOfSunshine.png");
            m->setParent(this);
            if (this->currMess != NULL){
                this->currMess->close();
                delete this->currMess;
            }
            this->currMess = m;
        }
    });

```

下面的代码一样不可以，定位说明极其不清楚
```
    // 在 connect(timer, &QTimer::timeout, [=](){ ... 
    // 这里的代码应该插入在僵尸移动逻辑之前，比如在 bossBuffActive 逻辑之后

    // === 新增：处理僵尸减速状态恢复 ===
    for (int i = 0; i < zombies.size(); i++) {
        if (zombies[i]->isAlive && zombies[i]->isSlowed) {
            zombies[i]->slowTimer -= 20; // timer 间隔是 20ms
            if (zombies[i]->slowTimer <= 0) {
                // 恢复速度
                double currentSpd = zombies[i]->getMoveSpeed();
                zombies[i]->setMoveSpeed(currentSpd / zombies[i]->appliedSlowFactor);
                zombies[i]->isSlowed = false;
                zombies[i]->appliedSlowFactor = 1.0;
            }
        }
    }

```

下面给出一个很好的例子,定位说明很清楚，代码没有省略中间部分。
```

在 `GameScene` 构造函数中找到 `spikeweedButton` 的代码块，在其后、`shovel` 按钮之前添加 `icebottleButton` 的代码。
请找到以下代码段：
```cpp
    connect(spikeweedButton,&QPushButton::clicked,this,[=](){
        if (sunNum>=this->gameConfig->spikeweedConfig.cost && timer->isActive()){
            preparedPlant="Spikeweed";
        }
        if (sunNum<this->gameConfig->spikeweedConfig.cost){
            qDebug()<<"没有足够阳光种地刺";
            Message * m = new Message(620,40,":/res/lackOfSunshine.png");
            m->setParent(this);
            if (this->currMess!=NULL){
                this->currMess->close();
                delete this->currMess;
            }
            this->currMess=m;
        }
    });
    QPushButton * shovel=new QPushButton(this);
```
将其替换为：
```cpp
    connect(spikeweedButton,&QPushButton::clicked,this,[=](){
        if (sunNum>=this->gameConfig->spikeweedConfig.cost && timer->isActive()){
            preparedPlant="Spikeweed";
        }
        if (sunNum<this->gameConfig->spikeweedConfig.cost){
            qDebug()<<"没有足够阳光种地刺";
            Message * m = new Message(620,40,":/res/lackOfSunshine.png");
            m->setParent(this);
            if (this->currMess!=NULL){
                this->currMess->close();
                delete this->currMess;
            }
            this->currMess=m;
        }
    });
    // === 新增冰瓶子炮按钮 ===
    QPushButton * icebottleButton = new QPushButton(this);
    icebottleButton->resize(90,120);
    icebottleButton->move(470,40);
    icebottleButton->setFlat(true);
    icebottleButton->setStyleSheet("QPushButton{border:none;background:transparent;}");
    connect(icebottleButton, &QPushButton::clicked, this, [=](){
        if (sunNum >= this->gameConfig->icebottleConfig.cost && timer->isActive()){
            preparedPlant = "icebottle";
        }
        if (sunNum < this->gameConfig->icebottleConfig.cost){
            qDebug() << "没有足够阳光种冰瓶子炮";
            Message * m = new Message(620,40,":/res/lackOfSunshine.png");
            m->setParent(this);
            if (this->currMess != NULL){
                this->currMess->close();
                delete this->currMess;
            }
            this->currMess = m;
        }
    });
    // === 结束新增 ===
    QPushButton * shovel=new QPushButton(this);
```

```
4. 其他细则 
- 无论如何给出代码，不允许减少头文件的包含，以免不必要的出错
- 请格外注意成员是否是保护成员，如果需要调用保护成员请自行添加方法，减少不必要的编译报错